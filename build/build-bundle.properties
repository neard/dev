# The bundle name
# Example: apache, php, phpmyadmin git, etc...
#bundle.name = apache

# The bundle release (default r1)
bundle.release = r1

# The bundle type
#bundle.type = bins

# The bundle format for compression. Can be zip or 7z (default 7z)
bundle.format = 7z
